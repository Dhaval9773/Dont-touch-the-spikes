using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Myenum
{
    spike,
    rightside,
    leftside,
    egg,
}
public class Enumrator : MonoBehaviour
{
    public Myenum identifier;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
